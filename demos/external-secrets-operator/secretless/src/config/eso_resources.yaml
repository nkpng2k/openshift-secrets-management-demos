apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: vault-backend
  namespace: demo-namespace-admin
spec:
  provider:
    vault:
      server: "http://VAULT_SVC_CLUSTER_IP:8200"
      path: "kv"
      version: "v2"
      auth:
        # Authenticate against Vault using a Kubernetes ServiceAccount
        # token stored in a Secret.
        # https://www.vaultproject.io/docs/auth/kubernetes
        kubernetes:
          # Path where the Kubernetes authentication backend is mounted in Vault
          mountPath: "kubernetes"
          # A required field containing the Vault Role to assume.
          role: "eso-role"
          # Optional service account field containing the name
          # of a kubernetes ServiceAccount
          serviceAccountRef:
            name: "eso-demo-sa"
            namespace: "demo-namespace-admin"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vault-external-secret-user
  namespace: demo-namespace-user
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: vault-secret-example
  data:
  - secretKey: password
    remoteRef:
      key: secret
      property: password
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vault-external-secret-admin
  namespace: demo-namespace-admin
spec:
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: vault-special-secret-example
  data:
  - secretKey: special-password
    remoteRef:
      key: special
      property: special-password
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: eso-demo-crb
  namespace: demo-namespace-admin
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: system:auth-delegator
subjects:
  - kind: ServiceAccount
    name: eso-demo-sa
    namespace: demo-namespace-admin
